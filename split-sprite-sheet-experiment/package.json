{
  "type": "module",
  "scripts": {
    "build": "rm -rf dist;node esbuild.js;",
    "serve": "php -S localhost:5500 -t dist/",
    "format": "prettier --write \"{,!(node_modules|dist|build)/**/}*.*\""
  },
  "dependencies": {
    "html2canvas": "^1.3.2"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.22",
    "@web/dev-server-esbuild": "^0.2.14",
    "@web/dev-server-legacy": "^0.1.7",
    "esbuild": "^0.12.28",
    "fs-extra": "^10.0.0",
    "prettier": "^2.4.1"
  }
}
